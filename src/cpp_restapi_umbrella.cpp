// Stub source for the cpp_restapi umbrella library.
// The umbrella aggregates OBJECT libraries (cpp_restapi_core and the
// optional cpp_restapi_json_pagination / cpp_restapi_github targets);
// CMake requires at least one source file, so this translation unit
// exists solely to satisfy that requirement.
